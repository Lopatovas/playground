## Practice HTML & CSS in isolation

These steps rehearse DevTools inspection, semantic tags, flexbox, CSS variables, and responsive breakpoints. Work in a **scratch folder** with a single `index.html` and `styles.css` — not your main project yet.

Open each file in Chrome, use DevTools Elements to verify your work, then answer the checkpoint.
