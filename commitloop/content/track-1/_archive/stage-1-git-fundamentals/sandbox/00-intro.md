## Practice the daily loop

These steps rehearse the moves you'll repeat every day: reading status, staging deliberately, writing clean commits, and working on branches. Use a **scratch repository** so you can experiment freely:

```bash
mkdir git-drills && cd git-drills && git init
```

Work through each step in your terminal, then answer the checkpoint to lock in the idea.
