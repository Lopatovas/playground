## Arrays as temporary storage

```javascript
const workouts = [{ id: 1, name: "Run", duration_min: 20 }];
```

Push to add, filter to remove, `.map()` to render. Same patterns you'll use when data comes from an API — only the source changes.
