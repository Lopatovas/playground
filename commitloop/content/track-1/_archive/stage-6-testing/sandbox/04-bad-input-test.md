## Only testing the happy path

Your suite has one test: valid POST → 201. Someone removes validation during a refactor. Tests stay green. Production stores empty names.

What test would have caught that?
