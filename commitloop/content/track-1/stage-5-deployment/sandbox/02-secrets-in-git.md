## The commit you can't undo

Someone pushes `API_KEY=sk_live_...` to GitHub. Even if they delete it in the next commit, it's in history. Bots scan for this within minutes.

Where should production secrets live instead?
