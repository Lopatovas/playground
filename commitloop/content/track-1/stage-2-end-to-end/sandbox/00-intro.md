## Think through the full stack

Before you build your slice, make sure the moving parts click. These checkpoints walk the path of a request — method, query, status, response — so that when you wire your own feature together, you know what each layer should do.
