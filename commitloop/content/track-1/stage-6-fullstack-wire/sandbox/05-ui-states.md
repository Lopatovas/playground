## The blank white screen

A user opens your workouts page. fetch is still running — nothing renders yet.

Then the API errors. Then it returns an empty array.

List the states a data-fetching view **must** handle to feel professional.
