## 201 Created

After a successful POST that creates a resource, `201` is more precise than `200`. Clients and tools can rely on that signal.

Use `curl -v` to see the status line in the response headers.
